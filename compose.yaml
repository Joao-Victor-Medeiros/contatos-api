services:
  consulta-cpf:
    build:
      context: .
      dockerfile: Dockerfile

    container_name: consulta-cpf

    ports:
      - "8080:8080"

    environment:
      SQLITE_PATH: "/data/${SQLITE_DB_NAME}"

    volumes:
      - "./database:/data"